# .fork-sync-ignore — upstream commits fork-sync must skip.
# One SHA per line, optional reason after whitespace.
# Blank lines and # comments allowed.
#
# See CLAUDE.md in pmin-mcpinfrastructure for the full convention.

# --- SSRF security fix (2026-06-07) ---
# These commits were already cherry-picked into our fork in a prior sync
# (as 34bac33 and 6efb3a0). The patches differ slightly so git --cherry-pick
# doesn't detect equivalence — add here to prevent re-apply conflicts.
66e5017  already applied as 34bac33 (fix(security): add retry limit on 429 and URL validation for SSRF protection)
42ec5b3  already applied as 6efb3a0 (fix(security): address Gemini Code Assist review feedback)

# --- mcp-trello: search_labels + remove_label_from_card (2026-07-26) ---
# Upstream 6a99ce4 adds search_labels and remove_label_from_card tools.
# Already applied to our fork as f3b55b1 (cherry-pick with minor patch divergence).
6a99ce4ebee255a072af99eb25f16e38031d3fb8  already applied as f3b55b1 (feat: add search_labels and remove_label_from_card tools)

# --- mcp-trello: Release 1.8.0 (2026-07-31) ---
# Applied manually as 44a01ae with conflict resolution — kept our custom-field
# and search_labels tools alongside upstream's new watch/update tools.
# Patch diverges from upstream so cherry-pick equivalence won't detect it.
5806416  applied as 44a01ae (Release 1.8.0 with manual conflict resolution — custom-field tools preserved)

# --- mcp-trello: wip pr-crusher dev snapshots (2026-07-31) ---
# Three rotating dev-snapshot commits that only modify .pr-snapshot.md.
# Upstream ultimately deletes the file in bf31af5 (applied as e289d8f).
# No functional content — skip to avoid repeated .pr-snapshot.md conflicts.
4a14ad3  wip dev snapshot — upstream deletes it in bf31af5 (already applied)
db1ff93  wip dev snapshot — upstream deletes it in bf31af5 (already applied)
bf31af5  applied as e289d8f (git rm .pr-snapshot.md + cherry-pick --continue; patch diverges)
6aef910  applied as 5d90d6f (ci: test+coverage gate; kept our functions threshold 32.77 vs upstream 32.66)

# --- mcp-trello: harden test and release (2026-07-31) ---
# 41bc348 includes searchLabels refactoring to use getBoardLabels() helper.
# Conflict resolution kept our custom-field methods AND took upstream's cleaner
# searchLabels refactoring. Patch diverges from upstream. Applied as 1a6445e.
41bc348  applied as 1a6445e (ci: harden test and release; searchLabels resolution diverges — took upstream getBoardLabels() refactor)

# --- mcp-trello: Release 1.8.1 (2026-07-31) ---
# All non-README changes were already present in our fork (no-op cherry-pick).
# README.md conflict resolved by keeping HEAD (our hosted MCP Registry install
# section, not the upstream npm/bunx quickstart).
# Merge commit c3e1e92 auto-skipped as a merge commit; 2a6e2cf is the release SHA.
2a6e2cf  no-op in fork — all changes already present; README kept HEAD (hosted deploy, not npm)

# --- mcp-trello: pre-1.8.0 PR commits already in fork (2026-08-11) ---
# These individual PRs were merged upstream before 1.8.0 and their functional
# content was already cherry-picked into our fork via earlier syncs.
da9f4a2  bundled in 1.8.0 (applied as 44a01ae) — feat: watch_card + watch_list
b37587a  already applied as bc3a70e — refactor: clean up download_attachment style + README docs
b40a232  already in fork — feat: get_card_attachments + get_card_checklists
7585281  already in fork — feat: support dueReminder for card create/update
3c9892c  already in fork — test: cover dueReminder
a08f6db  README/docs only — stale contributing links; keep HEAD
84d7721  npm metadata only (package.json); not relevant to our docker deploy
df37120  upstream planning doc only — no functional content
42fcb4c  pre-SSRF dev churn: mcp-evals devDependency move — skip
e352df8  pre-SSRF dev churn: mcp-evals devDependency move (duplicate) — skip
e60d25f  pre-SSRF dev churn: mcp-evals devDependency move — skip
02eee7b  pre-SSRF dev churn: mcp-evals devDependency move (duplicate) — skip
7df9223  pre-SSRF dev churn: fix bin path in package.json — skip

# --- mcp-trello: post-1.8.1 CI pipeline commits (2026-08-11) ---
# All touch only .github/workflows/ or .agents/skills/ (upstream npm release
# pipeline). We removed upstream CI in c038b90 — these are irrelevant to our
# docker/droplet deploy.
5cfa56f  upstream CI only — fix publish-script recursion + skip-if-published guards
f13c909  upstream CI only — Trusted Publishing (OIDC) for npm; drop NPM_TOKEN
0b79e9f  upstream CI only — prerelease-aware release tooling + registry beta guard
2407050  upstream CI only — make set-version prerelease group non-capturing
5d120de  upstream agent-skill docs only — publish-auth failure mode; not our concern
